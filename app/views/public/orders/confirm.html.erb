<h2>注文情報確認</h2>
<table>
  <thead>
    <tr>
      <th>商品名</th>
      <th>単価(税込)</th>
      <th>数量</th>
      <th>小計</th>
    </tr>
    <tbody>
      <% @order each do |order| %>
      <tr>
        <td><%= order.ordered_products.name %></td>
        <td><%= order.tax_price %></td>
        <td><%= order.ordered_products.amount %></td>
        <td><%= order.tax_price*ordered_products.amount %></td>
      </tr>
      <% end %>
    </tbody>
  </thead>
</table>